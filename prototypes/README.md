# 历史交互原型

这里保存早期独立 HTML 页面，用于回顾交互和视觉方案，不参与当前服务构建或生产运行。

- `index.html`：早期 Agent 调度台原型。
- `codex-fleet-demo.html`：工作流控制台交互演示。
- `role-management.html`：角色管理页面原型。

当前正式页面位于两个 Java 模块的 `src/main/resources/static/` 目录。
