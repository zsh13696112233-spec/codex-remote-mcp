# 停止模板编号更新与远程派发排查

## 环境与目的

- Windows / PowerShell，北京时间 2026-09-11。
- 分支：`codex/dingtalk-sop-target-v2`；提交前基线：`8d3da03`，已推送。
- 用户要求提交推送当前全部改动，并将本次会话总结写入 history。本文与下述两处已有改动一并提交；编写时尚未提交，最终提交号以 Git 日志为准。
- 前一阶段完整交接见 `history/2026-09-10_18-08-31_等待卡片控制与原会话返工修复.md`，不覆盖旧总结。

## 已完成修改

- 用户提供新增停止按钮的模板编号 `82792b55-07fb-4c4e-a579-60b16f3da560.schema`。
- 已同步 `DingTalkWaitingCard.TEMPLATE_ID` 与配置中心 README，替换旧编号 `59418790-6cad-43f9-b1d8-55cf9f18ed1a.schema`。
- 本轮没有修改派发逻辑、运行状态或执行机配置，没有重启服务，没有改变业务工作流。
- 前阶段返工恢复原会话、停止控制拦截、通知队列锁竞争修复已经包含在基线提交中，本次不是重新实现这些功能。

## 验证与 Review

- 编号更新后已运行：`mvn -q -f services/role-task-config-center/pom.xml package "-Dtest=DingTalkWaitingCardTest"`，6 项卡片测试通过，打包成功。Windows 使用现有 Maven 安装；macOS 按仓库规则使用 mvnd。
- 产物：`services/role-task-config-center/target/role-task-config-center-0.1.0.jar`，不提交构建产物。
- 本次提交前检查差异，只有模板常量、README 编号及本总结；沿用已经完成的针对性验证，不重复运行无关测试。
- 本次 review 未发现需要修复的问题；提交前检查 `git diff --check`。
- 新模板在真实钉钉中的停止、取消和三模式显示效果尚未取得用户验收结果。

## 仅排查发现：远程步骤启动异常

用户报告远程执行机只看到主监督和任务助手，未看到业务任务正常执行。读取中央公开状态及事件后确认：

- 该次运行主监督在 9 月 10 日 18:20 启动，派发及查询工具多次等待 300 秒超时。
- 18:38:14 才记录步骤派发和运行状态；18:38:15 收到业务侧会话创建、执行轮次开始事件。
- 18:38:21 主监督执行服务连接异常断开（1006），工作流标为失败；第一步仍记录运行中，后续步骤未启动。
- 不能以主监督“正在进行”的文字或早期 `node.started` 记录证明业务已正常执行。虽有会话/轮次启动事件，但没有业务执行内容或产出证据。
- 代码在登记执行任务时就记录 `node.started`，其含义早于实际业务产出。失败后的步骤状态收尾也需要继续排查。
- 未取得远程 app-server、工作流工具和 Sidecar 日志，无法确定工具阻塞和连接断开的根因；不能归因于返工恢复会话，也不能排除最近改动的回归。
- 已建议用户确认无遗留任务后仅重试一次；本会话没有收到新的重试编号或成功证据。上述问题仅排查，未修复。

## 后续事项

1. 重启 8091 后新生成卡片使用新模板编号；此处未替用户操作重启。
2. 停止拦截和原会话返工依赖前阶段 Python 中央及执行机更新，不能只更新配置中心。
3. 若派发异常重现，收集远程对应时段的工具、Sidecar 和 app-server 日志，区分派发前阻塞、会话启动、实际执行和状态回写。不要只根据模型文字判断进度。
4. 不在活动运行中贸然重启 Python 网关；其已有重启恢复规则可能将遗留运行标为失败。

本总结不保存原始业务对话、数据库内容或凭据。代码及总结按用户授权推送到现有 origin 分支。
