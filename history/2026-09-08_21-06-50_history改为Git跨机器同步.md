# history 改为 Git 跨机器同步

- 环境：macOS；目标是与 Windows 交接。
- 分支：`codex/dingtalk-sop-target-v2`；本次修改前基线：`81a1c56`。
- 用户明确把原先“history 不提交”的要求改为“history 提交到 Git”。最新要求优先，旧总结保留作历史记录。
- 已移除 `.gitignore` 中 `/history/` 规则，恢复该文件原有内容；AGENTS 和根 README 改为跨机器交接，开始时同步同一分支并读取最新日期命名总结。总结使用北京时间，记录操作系统、基线提交、实际修改、验证和未完成事项，路径优先仓库相对路径。
- 本次提交范围为 AGENTS.md、根 README.md 和 history/，包含上一轮对这两个文档的准确性修正及审查记录、两个合成数据复现脚本。上一轮监控中心 README 的接口数量修正仍留在工作区，未包含在本次提交范围内。
- 不自动推送。本地提交后仍需推送远程，Windows 同步同一分支才能读取这些记录。
- 业务代码未改。上一轮发现的四项问题尚未修复；详细证据和测试结果见上一条审查记录。本次仅调整文档及版本管理规则，不重复运行业务测试。
- 校验：git diff --check；确认 history 不再被忽略；提交前检查暂存文件范围。
- 图片复现脚本的 macOS 命令见脚本头部。Windows PowerShell 等价命令：

```powershell
$env:PYTHONPATH = "$PWD\services\python-workflow\src;$PWD\services\python-workflow"
.\.venv\Scripts\python.exe history/reproduce_image_budget.py
```

- 下一步：先确认两台机器已同步同一分支，再按上一条审查记录处理消息重试顺序、图片返工容量、网页返工后的钉钉跟踪及全表读取问题。
